// CSA tenant — analyze-only (no plan/dashboards modules), so no client content.
import type { Client } from "@/data/media-model";

export const clients: Client[] = [];
export const KNOWN_CLIENT_CODES = [] as const;
